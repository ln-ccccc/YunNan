# HuggingFace PyTorch Environment Dependencies
# Install in HFPyTorch310 conda environment

# GPU Worker 的精确 CUDA 构建由 docker/Dockerfile.inference-gpu 安装。
# 此处仅记录与派生镜像一致的 Python 版本约束。
torch==2.7.0
torchvision==0.22.0

# HuggingFace Transformers
transformers>=4.30.0

# Image processing
pillow>=9.0.0
numpy>=1.21.0
