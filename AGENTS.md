# 项目级开发规则

本文件是 YunNan 仓库的项目级开发约束。任何开发者、Codex 会话或自动化 Agent 在**阅读、修改或新增代码、配置、接口、部署脚本之前**，必须先阅读本文件；如目标目录还有更近的 `AGENTS.md`，还必须读取并遵守其中不冲突的规则。

详细架构契约以 [项目结构与低耦合契约](docs/architecture/project-structure-and-low-coupling-contract-v1.md) 为准。本文件只保留稳定、可执行的规则，不复制实现细节。

## 1. 目标与范围

- 本项目的长期目标是降低模块间耦合，使每个模块都有明确职责、输入输出和独立测试方式。
- 本期首先治理项目工作台（Project Hub），但不得借此无边界重构算法、训练、旧 GeoView 或部署系统。
- 正确性、可验证性、兼容性优先于“重构得更漂亮”。不为未来假设增加通用平台、插件机制或抽象层。

## 2. 修改前必做检查

每次改动前，执行并在工作说明或 PR 中写清：

1. 已阅读本文件及目标模块的相关架构契约。
2. 本次改动的唯一主模块、输入、输出、依赖者和最小验证命令。
3. 是否改变 HTTP API、状态字段、数据格式、物理目录或部署变量。
4. 若改变跨模块契约，先更新契约与 fixture，再分别实现提供方和消费方；禁止直接静默改接口。

如果无法说明“这个模块负责什么、谁消费它、如何独立测试”，先停止编码并补齐设计或向负责人确认。

## 3. 模块边界与依赖方向

```text
Miner Web (Vue) -> Miner BFF (Node/Express) -> Flask HTTP API
                                             -> Project Hub / Spatial / Results / Inference
```

| 模块 | 唯一职责 | 禁止事项 |
| --- | --- | --- |
| `miner/src/` | 项目工作台与地图交互；只渲染公开 DTO | 读取数据库、服务器物理路径或重复计算项目就绪规则 |
| `miner/routes/`、`miner/services/` | 同源 BFF、鉴权转发与轻量 HTTP 适配 | 承担 GIS、项目状态或推理业务决策 |
| `backend/applications/project_hub/` | 项目领域、项目概览、资产只读模型、活动审计 | 加载模型、执行训练或由前端路径决定存储位置 |
| `project_hub/spatial_*` | 矿山/底图资源、空间任务、瓦片激活 | 读取或修改分类成果版本 |
| 推理运行时 | 固定模型任务与暂存输出 | 改写正式项目成果或项目业务状态 |
| 成果/版本模块 | 推理成果发布、矢量修订、导出来源追溯 | 重新加载模型或修改原始影像 |
| 部署与离线包 | 镜像、挂载、健康检查、恢复演练 | 修改业务领域规则 |

只允许沿公共 HTTP、DTO、受控存储接口依赖。禁止前端绕过 BFF/后端访问 `project_storage`，禁止模块之间通过私有表结构、私有路径或内部函数形成隐式耦合。

## 4. 四层状态模型

下列状态必须分开命名、存储和计算，禁止用一个 `status` 混用：

1. `lifecycle_status`：项目业务生命周期，持久化值为 `draft`、`active`、`completed`、`archived`。现有 `Project.status` 在兼容期内仍保留；新增 API 可同时返回语义化别名。
2. `readiness`：项目工作流准备情况，值为 `blocked`、`partial`、`ready`，只读且由后端统一计算。
3. `ProjectAssetView.status`：单个资源状态，值为 `registered`、`processing`、`ready`、`failed`、`superseded`；首期可由现有资源记录映射，无需先建通用资产表。
4. `Job.status`：后台任务自身状态。沿用现有公开任务语义（如 `queued`、`running`、`succeeded`、`failed`、`cancelled`），不得为统一命名而破坏既有接口。

`lifecycle_status` 不等于 `readiness`。例如，项目可以处于 `active`，但因缺少激活底图而为 `blocked`。

## 5. 后端规则所有权

- 项目检查项、阻塞原因、下一步业务动作的选择与排序均由后端计算。
- 前端只消费 `readiness`、`checks`、`blockers` 与 `next_actions`，负责中文展示和界面导航；不得在多个组件中复制 `if (!basemap)` 等业务判断。
- 后端输出语义化 `action_code`；前端把它映射为当前界面的按钮、文案和路由，避免领域服务依赖 Vue 路由结构。
- 活动记录持久化 `action_code + actor + target + result + payload`，UI 再本地化为中文；禁止把可变中文句子当作审计主数据。

## 6. 资产、文件与导出

- 项目资产统一通过 `ProjectAssetView` 对外读取；页面不得感知其底层来自空间资源、数据集、成果、导出或快照中的哪张表。
- 服务器物理路径只可由存储服务生成和解析。浏览器不得提交任意 `output_dir`、绝对路径或目录遍历片段。
- 导出只能写入项目 sandbox 的 `exports/<export_id>/`，并生成 `manifest.json` 记录项目、来源资产版本和校验信息。
- 当前“备份”应在 UI 中称为“项目配置快照”，除非实际打包并验证原始影像、瓦片、成果文件和数据库，否则不得宣称为完整项目备份。

## 7. 前端拆分规则

- 先冻结 `/overview`、`/assets`、状态与 DTO 契约，再拆分 `ProjectWorkspace.vue`；禁止只做视觉层的代码搬家。
- 主工作台负责当前项目上下文和一次数据装配；概览、流程、资产、活动、导出/快照使用明确子组件或 composable。
- 子组件只接收 DTO/回调，不各自请求同一项目状态，也不直接拼服务器路径。

## 8. 测试、文档与提交

- 每个模块 PR 至少运行该模块的现有单元测试、静态检查或构建；新增跨模块契约必须有成功、无数据、参数错误和失败状态 fixture。
- API、状态字段、目录、部署流程或数据格式变更必须同步更新相关文档与契约。
- 一个 PR 只承担一个主模块。跨模块需求按“契约 → 提供方 → 消费方 → 集成验证”拆分。
- 不提交模型权重、原始影像、运行数据、离线镜像、Docker 卷或真实密钥。

## 9. 本期明确不做

- 在线训练、权重上传训练、精度承诺或模型重训。
- React 重写、浏览器 GB 级上传/切片、通用项目管理平台。
- 完整 RBAC、多人实时 GIS 编辑、审批流、工时或通用任务看板。
