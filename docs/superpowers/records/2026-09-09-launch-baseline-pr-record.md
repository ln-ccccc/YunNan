# 上线基线加固 PR 记录（2026-09-08 ~ 09-09）

本批次共 16 个提交，目标一句话：**在真实部署环境里把系统完整跑通一遍，修掉所有"只有上线才会发现"的问题，并落地优化建议文档中的两个短期功能。** 每个提交按「现象 → 改了什么 → 不改会怎样 → 怎么验证的」记录；详细版本见各自提交说明。

## 一、部署与环境类（不做就上不了线）

| 提交 | 一句话功能 | 不改的后果 |
| --- | --- | --- |
| `81cab81` fix(部署) | 统一脚本换行符为 Linux 格式 | Windows 拉代码部署：后端/切片/推理容器全部反复重启，系统完全不可用，报错还不提示真实原因 |
| `0a1b06c` fix(推理) | 推理容器启动时自动纠正坐标系资源路径 | 页面发起的推理全部失败（EPSG 找不到），核心功能不可用 |
| `57fa9e1` chore(部署) | 忽略本机专用部署配置文件 | 各人本地路径互相覆盖、误提交 |

## 二、正确性类（不做就会丢数据 / 卡死系统）

| 提交 | 一句话功能 | 不改的后果 |
| --- | --- | --- |
| `0e3be1d` fix(成果) | 存矢量成果和任务汇总的字段容量扩大 256 倍（TEXT→MEDIUMTEXT），启动时自动升级老库 | 565 座矿山的矢量成果随机丢失（实测 6.7 万字节就超限）；任务永久卡"运行中"，需人工救 |
| `9a49225` fix(影像) | 修复 IMG/JP2 登记后卡"已登记"、推理页选不到 | 新格式功能形同虚设：登记成功却永远不能推理 |
| `4d79c2f` fix(页面) | 两个同名按钮改出名 + 加固定标记 | 自动化/辅助工具"点了没反应"（误中隐藏按钮）；用户混淆入口与确认 |
| `6e4da79` fix(空间) | 底图切片服务加任务级兜底 | 切片中一个坏任务停摆所有排队切片，只能重启容器 |
| `333dfc8` 前一版 `71d11e6` fix(推理) | 推理服务加任务级兜底 | 一个坏任务＝全部任务停摆＋每次重启重载 2GB 模型中断数分钟 |

## 三、新功能类（对应优化建议文档）

| 提交 | 一句话功能 | 输入 → 输出 |
| --- | --- | --- |
| `c284852` feat(影像) | 影像登记/推理支持 IMG、JP2 格式 | 登记 `incoming/xx.img|.jp2` → 进入就绪列表可推理；不再需要提前手工转格式 |
| `333dfc8` feat(成果) | 一键台账 Excel 导出 | 点「导出 XLSX」→ 项目受控目录生成两个工作表（成果台账 + 地类图斑统计），带清单与校验值 |

## 四、清理类（删的都是确认没人用的）

| 提交 | 删了什么 | 依据 |
| --- | --- | --- |
| `69e9954` chore(页面服务) | 6 个只为已停用全局接口服务的转发模块 + 坏脚本入口（536 行） | 逐文件全仓引用检索为零 |
| `1b6ea56` chore(后端) | 其他项目的样例数据生成脚本（204 行） | 目录结构属于通用分割项目，全仓零引用 |
| `4b520a1` chore(推理) | 江西部署机的写死 Python 路径 | 本机与容器均不存在、永不命中 |

## 五、测试口径类（让测试说真话）

| 提交 | 内容 |
| --- | --- |
| `1dd84b2` | 镜像名单期望改回云南实际值（原为江西旧名，长期假失败） |
| `a8d111f` | "脚本禁止调用 conda"的检查放宽到本意（注释中的说明文字不再误报） |

## 验证基线（本批次的统一验收结论）

- 后端全量 305 项：除 1 项已知环境项（web 镜像不含 mmseg，归 worker 镜像职责）外全部通过，10 项条件性跳过均为预期；
- miner 45 项 + 构建通过；
- 真实栈（旧数据库 + 真实权重）全业务链走通：登录正负向 → 建档 → 矿山/底图 → 登记 IMG 影像 → GPU 推理成功 → 矢量成果就绪 → 台账/GeoJSON 导出 → 配置快照 → 会话过期自动回登录页；
- GPU 推理连续两任务稳定成功（cuda:0，无回退）。
