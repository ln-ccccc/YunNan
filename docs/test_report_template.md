# 测试报告模板

按 [开发规范](development-standard.md) 选取本次影响范围，不要求每次填写全部场景。未执行、跳过与通过必须分开。

## 基本信息

- 测试日期：
- 测试人：
- 分支/提交：
- 工作树路径 / 是否含未提交改动：
- 测试范围：
- 环境：Node/npm、Python/GDAL、浏览器；涉及 Docker/GPU 时补镜像 ID、权重标识和设备。
- 数据：合成 / 脱敏 / 真实，项目/要素/坐标位置数量，影像像素/波段/ROI；不要附密钥。

## 测试结果总览

| 结论 | 数量 |
| --- | ---: |
| 通过 | 0 |
| 失败 | 0 |
| 跳过 | 0 |
| 未执行 | 0 |

## 测试明细

| 测试项 | 命令或操作 | 结果 | 失败原因 | 备注 |
| --- | --- | --- | --- | --- |
| Miner 测试 | 在 `miner/` 执行 `npm test` | 未执行 |  | 记录数量/退出码 |
| Miner BFF 语法 | 在 `miner/` 执行 `node --check server.js` | 未执行 |  | 仅语法 |
| Miner 构建 | 在 `miner/` 执行 `npm run build` | 未执行 |  |  |
| GeoView 测试与构建 | 在 `frontend/` 分别执行 `npm test`、`npm run build` | 未执行 |  | 逐条记录 |
| 后端对应模块测试 | 在固定 GIS 环境填写实际 unittest 命令 | 未执行 |  | mock/临时库范围 |
| 项目与空间流程 | 建项目、自定义 FID、底图切片激活、失败重试 | 未执行 |  | 浏览器 + 实际 API |
| 项目推理 | 登记 TIFF → `/api/inference/jobs` → 轮询 → 读取成果 | 未执行 |  | 明确 mock 或真实 worker |
| GeoView 编辑 | 改类/几何/增删 → 保存 → 重开 → 导出并解析 | 未执行 |  | 校验当前版本和自动基线 |
| 冲突与错误恢复 | 409/422、切项目、断网、重复点击 | 未执行 |  | 无串数据、无部分覆盖 |
| 真实模型运行 | 固定影像/权重/设备，记录任务与成果 | 未执行 |  | CPU/GPU分别说明 |
| 部署演练 | 独立环境启动、健康检查、重启/恢复 | 未执行 |  | 不使用生产卷 |

## 性能记录（仅涉及性能时填写）

- 参考机/网络/资源分配：
- 基线提交 / 本次提交：
- 数据标识、规模、冷/热缓存、并发/后台任务条件：
- 采样方法与次数：

| 场景 | 初始目标 | 修改前 | 修改后 | 失败数 / 样本数 | 峰值资源 / 说明 |
| --- | --- | --- | --- | --- | --- |
| 例如常规项目切换 | p95 ≤ 3 s | 未测 | 未测 |  |  |

少量真实推理报告逐次耗时与中位数；不要用 3 次运行声称可靠 p95。若未实测，性能结论填“待测”，不填“满足要求”。

## 未测项与风险

- 未测项：
- 未测原因：
- 潜在风险：
- 建议后续检查：

## 结论

填写本次测试是否满足交付要求，以及是否存在需要阻塞发布的问题。

交付状态：设计 / 已实现 / 模块已验证 / 浏览器已验证 / 实机已验证 / 已合并 / 已部署。只填写有证据的状态；未测项继续保留。
